WellOne Customer v82
- Separate deployment; index.html is at ZIP root.
- Customer sees sizes only under the colours that actually have those exact stock variants.
- Exact colour + size stock is checked again during order confirmation.
- Offers, My Orders, cancellation/history and existing customer functions remain.
- New v82 cache namespace/assets replace v81 cache.
- Run supabase/09_realtime_exact_variant_sync.sql for strongest live inventory refresh.
