WELLONE CUSTOMER SITE - SEPARATE DEPLOYMENT

Deploy the CONTENTS of this folder as one independent site.
Main entry: index.html

This site contains customer shopping, cart, order confirmation and My Orders.
It uses the same Supabase database as the Admin, Employee and Order Receiving sites.

Before using the new order/employee/variant flow, run the shared Supabase SQL migration once.
